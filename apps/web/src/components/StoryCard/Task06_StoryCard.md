Purpose

- Present a single story item (StoryBase) with title, domain, points/comments, tags/topics, and summary excerpt.

Notes

- Server component. Accepts StoryBase + optional summary snippet.
- Link to discussion (HN) and original URL with rel attributes.
- Handle Ask HN (no URL) gracefully.

Blocked by

- types generation from contracts/openapi.yaml (StoryBase), CSS tokens.

